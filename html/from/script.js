
    function submitForm() {
      // Validate the form fields (add your validation logic here)

      // Display popup with selected values
      alert("Submitted!\nDisplay values in a popup here.");

      // Reset the form
      document.getElementById("surveyForm").reset();
    }

    function resetForm() {
      // Reset the form
      document.getElementById("surveyForm").reset();
    }


